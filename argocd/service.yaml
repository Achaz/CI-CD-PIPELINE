apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: argocd
spec:
  type: ClusterIP
  selector:
    app: my-ci-cd-app
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
